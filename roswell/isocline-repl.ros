#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

(progn
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload '(:isocline-repl) :silent t))

(defun main (&rest argv)
  (declare (ignore argv))
  (isocline-repl:main))
